{
  "name": "stacktrace-resolve",
  "version": "0.1.0",
  "description": "Turns a stack trace into code.",
  "scripts": {
    "prepublish": "npm test && npm run build",
    "build": "BABEL_ENV=production babel --presets=react-app src -d lib",
    "test": "echo \"There are no tests yet! :(\" && exit 0"
  },
  "main": "lib/index.js",
  "repository": "git@github.com:Timer/stacktrace-resolve.git",
  "author": "Joe Haddad <timer150@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "error-stack-parser": "1.3.6",
    "source-map": "0.5.6"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-react-app": "^1.0.0"
  }
}
